[project]
name = "llm-from-scratch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sarthak-builds", email = "sarthakshiroty04@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "tiktoken>=0.14.0",
    "torch>=2.14.0",
    "sentencepiece>=0.2.0",
]

[project.scripts]
llm-from-scratch = "llm_from_scratch:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
